apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: platform-api
  namespace: platform
  labels:
    app: platform-api
spec:
  selector:
    matchLabels:
      app: platform-api
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
